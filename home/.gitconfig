# This is Git's per-user configuration file.
[user]
	name = Freddy Montes
	email = freddymontes@gmail.com
# Please adapt and uncomment the following lines:
#	name = Freddy Montes
#	email = freddymontes@gmail.com
[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
[alias]
	gco = checkout
	co = checkout
	st = status
[credential]
	helper = store
